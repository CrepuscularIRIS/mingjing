"""Operational / audit scripts for MingJing (not part of the runtime package).

Marked as a package so audit tools like :mod:`scripts.audit_verbatim` can be
imported from tests. Contains no product runtime logic.
"""
