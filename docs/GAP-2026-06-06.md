# 明镜 / MingJing — Full GAP Analysis

> Date: 2026-06-06 · Branch: `feature/mingjing-w1-core` (not merged)
> Deadline: **6/10 submit** · 答辩 6/12–19
> Sources: `Race/Competition.md`, `Race/【CIS】AI 全栈项目挑战赛开题材料.md`, current code at `mingjing/`.
> Status: backend ~85–90% real; gaps concentrated in (1) one compliance landmine, (2) the frontend, (3) deliverable materials (录屏/docs).

---

## 🔴 GAP-0 — CRITICAL COMPLIANCE: wrong LLM provider

| Competition mandates | Code actually runs |
|---|---|
| **Doubao-Seed-2.0-lite** (官方提供, `EP ep-20260514111325-xjmj7`), via Volcengine Ark | **MiniMax-M2.7** via `https://api.minimaxi.com/v1` |

- Evidence: `config.py:87-89` defaults to `minimax_base_url=api.minimaxi.com`, `minimax_model=MiniMax-M2.7`. `.env` has a **misleading comment** claiming Doubao/Ark above MiniMax values.
- Hits scoring axis ⑤ (合规) directly: "工具、模型、数据的使用符合规范". A judge asking "用什么模型" gets a wrong answer live.
- Fix is small (Ark is OpenAI-compatible: change base_url + model + key) but **must be re-verified live** — Doubao's JSON / `<think>` behavior differs from MiniMax and the whole pipeline depends on JSON parse + think-strip (`llm.py`). Budget ~0.5 day.
- Old committed Doubao key still needs **account-side rotation** with organizers (repo scrub done).

---

## 1. FRONTEND GAP (rebuild decided: **Vite SPA + shadcn/ui**)

### 1a. Why the current frontend is weak
42 hand-rolled `.tsx` files in raw Tailwind, no UI library (deps: only `react`, `reactflow`, `recharts`). Recent FINDING-001/002/003 commits added Inter + Source Serif + an "ink/mirror" token scale, but the bones are a generic admin dashboard.

| # | Problem | Where | Impact |
|---|---|---|---|
| F1 | **Double left sidebar** — 288px run panel (`w-72`) + separate 112px nav rail (`w-28`), ~400px before content | `App.tsx:234-392` | Cramped, amateurish |
| F2 | **Nav rail too narrow** — `w-28` for CJK labels + `border-l-2` active state | `App.tsx:372-392` | Weak hierarchy |
| F3 | **Four stacked horizontal bands** — header → error banner → KpiBar → CredibilityPanel → tab-description | `App.tsx:396-409` | Content below fold |
| F4 | **2s polling, no streaming** | `App.tsx:157-175` | "Live" feels laggy; undersells 25% observability |
| F5 | **`Observability` view unmounted** — exists + tested, not in `Tab` union/nav | `App.tsx:37` | Built work scoring nothing |
| F6 | **DAG wrong** — ends at `write`; real graph is `write→synthesis→END` | `graph.py:286` | 口径不一致 at 答辩 |
| F7 | **Hero ledger hidden** behind collapsed `<details>` | `FinalReport.tsx:398` | Buries 35% content |
| F8 | **Generic chrome** — default recharts/reactflow, no skeletons/empty states/motion | throughout | AI-slop look |
| F9 | **Mixed CN/EN labels** — form English, nav Chinese | `App.tsx:266-301` | Consistency ding |

### 1b. Hero experiences the rebuild MUST preserve (backend already computes all of this)

| Experience | Endpoint (exists) | Scores |
|---|---|---|
| Final Report: BLUF→SWOT→comparison→recommendations→gaps, every sentence `[c1]`-cited | `GET /runs/{id}/report`, `…/synthesis` | 35%+20% |
| Evidence & 溯源: click claim → drawer → highlighted snippet → LIVE/CACHED + 可靠性评级 | `GET /sources/{id}` | 35% |
| **QA Replay (money shot)**: reject → re-collect → strengthen, with **before→after numeric delta** | `GET /runs/{id}/claims/{cid}/history` | 35% |
| Execution Trace DAG incl. **synthesis** node + per-node LLM/token | `GET /runs/{id}/trace` | 25% |
| Schema Matrix: competitor×field, strength-colored, domain-switchable | report + `getSchemas()` | 题目+20% |
| Observability: token/latency/budget per node | trace + `llm_calls` | 25% |
| Credibility panel: `repair_delta` headline + "真闭环确认" banner | `GET /runs/{id}/credibility` | 35% |
| KPI bar reframed to **"verified claims gained"** (1/5→4/5) | metrics | 20% |
| 问卷/访谈 design card + survey-source chips | `GET /runs/{id}/survey-design` | 题目-named |
| Correction controls (人工修正 HITL) | component exists | 20% |

API client contract to keep (`api/client.ts`): `createRun, getTrace, getReport, getSynthesis, getCredibility, getClaimHistory, getSource, getSchemas, getSurveyDesign`.

### 1c. Rebuild plan (Vite + shadcn/ui)
- **Stay Vite SPA**, add **shadcn/ui (Radix + Tailwind)** — reuse all existing API client + view logic; lowest risk before 6/10.
- **Do NOT copy DeerFlow** (Next.js chat/super-agent console; wrong IA, full rewrite). Borrow style only.
- **Template to hunt for:** a data-dense **analyst / BI workbench** (Linear / Vercel-dashboard / shadcn admin kit), single left sidebar, top command bar, card+table density, drawer pattern, flow canvas. Not a chat UI.
- **Keep** `reactflow` (DAG) + `recharts` (observability); just restyle.
- **IA fixes:** collapse double rail → one sidebar; promote ledger to default-open; animate trace arrivals to feel streaming.

---

## 2. FUNCTIONAL / BACKEND GAPS (by scoring axis)

Backend strengths (keep front-and-center): append-only ledger, deterministic QA gate, honest weak→strong (`source_cap = 1 + revision_round`), projection-forced 溯源, Evidence-Admissible authority boundary.

### ① 多 Agent 协作与可信度 (35%) — ~90%
- **[High]** 问卷/访谈 lane built but half-surfaced (the *one* title-named capability). Responses are Notion-only curated fixture (never synthesized). Surface 问卷设计 + 访谈, labeled demo-data.
- **[Med]** Prover/Refuter pure module landed but **NOT graph-wired** — don't pitch as live.
- **[Med]** QA Replay delta not numeric (shows "+N sources", not `evidence_strength` before→after) — quantify the 非伪闭环 proof.

### ② 技术深度与工程完整度 (25%) — ~85%
- **[High]** No clean **live** end-to-end rehearsal with the **correct model (Doubao)** under 6-min budget. Offline 4.2s proven; one live MiniMax run 130s.
- **[Med]** `Observability` unmounted (F5) — data exists, scores 25%, hidden.
- **[Low, documented]** value sub-fields LLM-asserted not all snippet-verified; SQLite single-lock (no concurrency — fine, organizers said nice-to-have); DNS-rebind TOCTOU in SSRF guard.

### ③ 业务价值与产品体验 (20%) — ~65%
- **[High]** KPIs not reframed to "verified claims gained" (closed-loop number, not source counts).
- **[High]** No quantified human-analyst baseline (rubric wants 可量化提升 in 效率/覆盖度/一致性, e.g. "16–40h → X min").
- **[Med]** Confirm `CorrectionControls` HITL truly feeds back into the run (real reverse-channel, not just UI).

### ④ 代码质量与文档 (10%) — ~65%
- **[High]** Docs lag code: `docs/architecture.md` graph still `write→END` (no synthesis/Evidence-Admissible/domain); `docs/agent-protocol.md` missing `VALUE_UNSUPPORTED`; `docs/ROADMAP.md` calls views "stubs".
- **[Med]** Required docs thin: 架构图, Agent 角色与协议文档, 部署说明 (rubric names these).
- **[Med]** TRAE / AI-coding 使用痕迹 (10% axis) — frame git history as AI-assisted / capture TRAE records.

### ⑤ 合规、材料与答辩 (10%) — ~50%
- **[Critical]** Doubao model (GAP-0).
- **[Critical]** 演示录屏 not recorded (推荐 6 min).
- **[High]** Demo run must force ≥1 reject→revise, else `repair_delta = +0%` and "真闭环确认" stays dark.
- **[Med]** PII free-text names not NER-removed; add 合规声明 (robots/服务条款) in docs.

---

## 3. Prioritized punch-list

**Must-do before 6/10:**
1. 🔴 Switch to Doubao-Seed-2.0-lite + re-verify live (GAP-0).
2. 🔴 Record 6-min 录屏 on a run that forces reject→repair.
3. 🔴 Frontend rebuild (Vite + shadcn/ui) preserving §1b hero experiences.
4. 🟠 Reframe KPIs to "verified claims gained" + human-baseline number.
5. 🟠 Sync 3 stale docs + write 架构图 / agent-protocol / 部署说明.

**High-ROI, cheap (fold into rebuild):** mount Observability; DAG synthesis node; numeric QA-Replay delta; surface 问卷/访谈; default-open ledger.

**Verbal-only at 答辩 (don't build):** dynamic DAG topology; true concurrency; Prover/Refuter live (unless wired).
