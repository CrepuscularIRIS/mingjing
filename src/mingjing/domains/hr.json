{
  "integration_matrix": {
    "required": ["supported_hris_platforms"],
    "sub_fields": ["supported_hris_platforms", "sync_type"]
  },
  "compliance_certifications": {
    "required": ["security_certifications"],
    "sub_fields": ["security_certifications", "ai_regulatory_status"]
  },
  "pricing_model": {
    "required": ["primary_billing_unit"],
    "sub_fields": ["primary_billing_unit", "tiers"]
  },
  "deployment_model": {
    "required": ["available_topologies"],
    "sub_fields": ["available_topologies", "default_topology"]
  },
  "user_persona": {
    "required": ["primary_personas"],
    "sub_fields": ["primary_personas", "buyer_persona"]
  }
}
