{
  "pricing_model": {
    "required": ["tiers", "billing_unit"],
    "sub_fields": ["tiers", "billing_unit", "free_tier", "currency"]
  },
  "autonomy_level": {
    "required": ["autonomy_tier"],
    "sub_fields": ["autonomy_tier", "hitl_requirement"]
  },
  "capability_matrix": {
    "required": ["primary_task_domains"],
    "sub_fields": ["primary_task_domains", "benchmark_scores"]
  },
  "integration_ecosystem": {
    "required": ["protocol_support"],
    "sub_fields": ["protocol_support", "native_connectors"]
  },
  "model_backbone": {
    "required": ["primary_llm"],
    "sub_fields": ["primary_llm", "context_window_tokens"]
  },
  "safety_guardrails": {
    "required": ["prompt_injection_defense"],
    "sub_fields": ["prompt_injection_defense", "audit_logging"]
  },
  "user_sentiment": {
    "required": ["overall"],
    "sub_fields": ["overall", "positives", "negatives"]
  },
  "swot": {
    "required": ["strengths", "weaknesses"],
    "sub_fields": ["strengths", "weaknesses", "opportunities", "threats"]
  }
}
